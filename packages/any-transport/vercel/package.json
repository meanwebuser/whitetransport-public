{
  "name": "y-transport-vercel",
  "version": "1.0.0",
  "description": "Y Transport — Vercel serverless webhook handlers",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "register": "curl -X POST http://localhost:3000/api/webhook/register -H 'Content-Type: application/json' -d '{\"base_url\":\"CHANGE_ME\"}'"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0",
    "typescript": "^5.3.0"
  }
}
